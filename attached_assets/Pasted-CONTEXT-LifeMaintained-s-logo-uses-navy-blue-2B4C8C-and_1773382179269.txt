CONTEXT: LifeMaintained's logo uses navy blue (#2B4C8C) and orange (#E8943A). The app currently uses teal (#00C9A7) as its accent color throughout. The entire color palette needs to be updated to match the brand identity.
Color psychology research basis: Blue builds trust and reliability (42% of users associate blue with trustworthiness). Orange drives action and boosts click-through rates by up to 24% on CTA buttons. The complementary blue-orange combination creates high visual contrast and energy. The University of Loyola, Maryland found consistent brand colors increase recognition by 80%.
IMPORTANT: Only modify constants/colors.ts and app.json. Do NOT modify any component files. Every component in the app already reads from the Colors constant, so changing the values in one file updates the entire app automatically.

TASK: Update the Colors constant in constants/colors.ts to use the brand palette.
Replace the current values with:
const NAVY = "#2B4C8C";
const ORANGE = "#E8943A";
const RED = "#FF453A";
const YELLOW = "#FFD60A";
const GREEN = "#32D74B";
const BLUE = "#4A90D9";

export const Colors = {
  background: "#0C111B",
  surface: "#131A2B",
  card: "#1A2236",
  cardElevated: "#212B42",
  border: "#2A3550",
  borderSubtle: "#1A2236",

  text: "#F0F2F8",
  textSecondary: "#8B93A8",
  textTertiary: "#5A6480",
  textInverse: "#0C111B",

  accent: ORANGE,
  accentMuted: "rgba(232, 147, 58, 0.15)",
  accentLight: "rgba(232, 147, 58, 0.08)",

  overdue: RED,
  overdueMuted: "rgba(255, 69, 58, 0.15)",
  dueSoon: YELLOW,
  dueSoonMuted: "rgba(255, 214, 10, 0.15)",
  good: GREEN,
  goodMuted: "rgba(50, 215, 75, 0.15)",

  blue: BLUE,
  blueMuted: "rgba(74, 144, 217, 0.15)",

  vehicle: ORANGE,
  vehicleMuted: "rgba(232, 147, 58, 0.15)",
  home: "#64D2FF",
  homeMuted: "rgba(100, 210, 255, 0.15)",
  health: "#FF6B9D",
  healthMuted: "rgba(255, 107, 157, 0.15)",

  tabBar: "rgba(12, 17, 27, 0.92)",

  white: "#FFFFFF",
  black: "#000000",
};
Key changes explained:

Accent color: teal (#00C9A7) → orange (#E8943A). This affects every CTA button, toggle, active tab, selected states across the entire app. Orange drives action.
Background/surface/card: shifted from gray-neutral (#0B0C10) to navy-tinted dark (#0C111B). This gives the entire app a cohesive navy undertone that echoes the logo's blue.
Vehicle category color: was separate orange (#FF9F0A), now uses the brand orange for consistency.
Text colors: slightly warmer to complement the navy backgrounds.
Tab bar: matched to new background with higher opacity for better legibility.

Also update the splash backgroundColor in app.json if not already done:
"backgroundColor": "#0C111B"
DO NOT modify any component files. The palette change propagates automatically through the Colors import.
VERIFICATION:

Open the app — overall feel should be navy-dark with orange accents
All buttons (Save, Process, Mark Complete, Generate Schedule) are orange instead of teal
Toggle switches use orange when active
Active tab indicator is orange
Dashboard category cards still have distinct colors (orange for vehicles, blue for home, pink for health)
Overdue/due_soon/good status colors are unchanged (red/yellow/green)
Text is readable against all backgrounds (check contrast)
The app feels cohesive with the logo's navy + orange brand identity
Zero TypeScript errors