/** Four radii plus pill. */
export const Radius = {
  /** Chips, badges, inputs. */
  sm: 8,
  /** List rows, buttons. */
  md: 12,
  /** Cards. */
  lg: 16,
  /** Sheets and hero surfaces. */
  xl: 24,
  /** Pills, avatars, the FAB. */
  pill: 999,
} as const;
