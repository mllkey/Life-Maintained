/** 4pt grid. Only these values appear in layout code. */
export const Spacing = {
  xs: 4,
  sm: 8,
  md: 12,
  lg: 16,
  xl: 20,
  xxl: 24,
  xxxl: 32,
  xxxxl: 40,
  /** Horizontal screen gutter. */
  gutter: 20,
  /** Vertical padding inside a list row. */
  rowVertical: 12,
  /** Gap between sections on a screen. */
  section: 24,
} as const;
